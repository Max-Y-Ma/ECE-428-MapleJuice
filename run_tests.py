import unittest
import subprocess
import os

# Run unit tests
# suite = unittest.defaultTestLoader.discover('tests')
# result = unittest.TextTestRunner().run(suite)
# FTP_DIRECTORY = f"{os.environ.get('PWD')}/data"
# result = subprocess.run(['python3', f"{FTP_DIRECTORY}/bin/maple_exe.py", f"{FTP_DIRECTORY}/STORY/Quill.csv", "story"], check=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE, universal_newlines=True)
